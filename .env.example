VITE_API_BASE_URL=https://new-backend-production.apps.konnectbd.com/api/v1

# Server-side only (set in Vercel Project → Settings → Environment Variables).
# NOT a VITE_ var — must never be exposed to the browser. Used by api/check-email.ts.
LISTCLEAN_API_KEY=your-listclean-api-key
